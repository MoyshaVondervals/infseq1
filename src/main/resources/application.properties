spring.application.name=infseq1
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5433/infseq}
spring.datasource.username=${DB_USERNAME:infseq}
spring.datasource.password=${DB_PASSWORD:infseq}
spring.jpa.hibernate.ddl-auto=update
security.jwt.secret=${JWT_SECRET}
security.jwt.expiration-seconds=${JWT_EXPIRATION_SECONDS:3600}
